- content_for :main do
  .row
    .col-lg-12
      %h1.page-header
        Current Events
        %small
          happening around Berkeley
  .row
    - @events.each do |event|
      = render :partial => "list_item", :locals => { :event => event }

  %hr/
  .row.text-center
    .col-lg-12
    
      = paginate @events
  


